# backend/nginx.conf
events {
    worker_connections 1024;
}

http {
    upstream vulor_app {
        server web:8000;
    }

    server {
        listen 80;
        server_name localhost;

        # Receipts are validated up to 5MB by Django (payments/forms.py);
        # nginx's own 1MB default would silently reject anything above
        # that before Django ever sees it.
        client_max_body_size 6M;

        # Security headers
        add_header X-Frame-Options DENY always;
        add_header X-Content-Type-Options nosniff always;
        add_header X-XSS-Protection "1; mode=block" always;

        # Static files
        location /static/ {
            alias /app/staticfiles/;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Payment receipts are customer bank documents — never served
        # directly; access goes through the staff-gated Django view
        # (/payments/receipts/<id>/).
        location /media/payment_receipts/ {
            deny all;
        }

        # Media files
        location /media/ {
            alias /app/media/;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Django application
        location / {
            proxy_pass http://vulor_app;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Host $host;
            proxy_redirect off;
        }

        # Health check
        location /health/ {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }
}