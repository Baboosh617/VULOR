diff --git a/.gitignore b/.gitignore
index 4afce3a..469bc39 100644
--- a/.gitignore
+++ b/.gitignore
@@ -24,6 +24,9 @@ node_modules/
 dist/
 build/
 
+# Django collected static (regenerated by collectstatic on every deploy)
+staticfiles/
+
 # media/
 
 # Logs
diff --git a/accounts/migrations/0004_alter_customuser_phone.py b/accounts/migrations/0004_alter_customuser_phone.py
new file mode 100644
index 0000000..e457199
--- /dev/null
+++ b/accounts/migrations/0004_alter_customuser_phone.py
@@ -0,0 +1,17 @@
+import django.core.validators
+from django.db import migrations, models
+
+
+class Migration(migrations.Migration):
+
+    dependencies = [
+        ('accounts', '0003_alter_customuser_phone'),
+    ]
+
+    operations = [
+        migrations.AlterField(
+            model_name='customuser',
+            name='phone',
+            field=models.CharField(blank=True, max_length=17, null=True, validators=[django.core.validators.RegexValidator(message="Phone number must be entered in the format: '+999999999'.", regex='^\\+?1?\\d{9,15}$')]),
+        ),
+    ]
diff --git a/dashboard/models.py b/dashboard/models.py
index 6c1d8d2..71a8362 100644
--- a/dashboard/models.py
+++ b/dashboard/models.py
@@ -1,9 +1,3 @@
 from django.db import models
 
-
-FIT_CHOICES = (
-    ('loose', 'Loose'),
-    ('fitted', 'Fitted'),
-)
-
-fit_type = models.CharField(max_length=20, choices=FIT_CHOICES, blank=True, null=True)
+# Create your models here.
